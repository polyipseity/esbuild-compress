---
"esbuild-compress": patch
---

Apply semicolons and consistent formatting to `index.js`.

Add trailing newlines to `.changeset` and `.markdownlint.json`.

Minor cleanup: use `globalThis.TextEncoder` and rewrap lines.
